#include <stdio.h>
#include <conio.h>
void main(){
    int a;
    if(3+2%5)
        printf("This works.");
        if(a=10)
         printf("This works too.");
         if(-5) printf("wow this works too. 🤨");
         if('a') printf("Worked.");
}